# Frontend is a static site on Render; the API base is set in src/lib/api.js
# (https://buyinstantkeys-backend.onrender.com). This file only applies on
# Netlify-style hosts — kept for SPA fallback compatibility.
/*    /index.html   200
